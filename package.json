{
  "name": "scubot-ppt",
  "author": "hx-w",
  "version": "1.0.0",
  "scripts": {
    "start": "fusuma start",
    "start:prod": "fusuma start-prod",
    "build": "fusuma build",
    "deploy": "npm run build && fusuma deploy",
    "pdf": "npm run build && fusuma pdf"
  },
  "devDependencies": {
    "fusuma": "^2.8.4"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hx-w/fusuma-demo.git"
  }
}
